#include "system/loggerd/decoder/decoder.h"

// This file exists to satisfy the SConscript build requirements.
// The VideoDecoder base class is purely abstract (header-only),
// with concrete implementations in ffmpeg_decoder.cc and v4l_decoder.cc
